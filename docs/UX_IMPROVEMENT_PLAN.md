# Modern Excel PMS UX/UI 改善計画

## 概要

本ドキュメントは、Modern Excel PMS の直感的な操作性向上のための改善計画をまとめたものです。

---

## 現状の課題

### 1. ナビゲーション
| 課題 | 影響 |
|------|------|
| ホーム画面がない | 初回利用時にどこから始めればよいか不明 |
| シート順が論理的でない | Config → Template → PRJ → Master の順が作業フローと不一致 |
| シート間の関係性が不明確 | Case → Measure → WBS の階層構造が見えにくい |

### 2. 入力体験
| 課題 | 影響 |
|------|------|
| 入力セル（薄青）の説明がない | どのセルが編集可能か分からない |
| 進捗率が小数入力（0.5=50%） | 直感的でなく入力ミスが発生しやすい |
| エラーメッセージが汎用的 | 何が間違っているか分かりにくい |

### 3. 視覚的階層
| 課題 | 影響 |
|------|------|
| ヘッダースタイルがシート間で不統一 | 一貫性のない印象 |
| セクション区切りが不明確 | 情報の区別がつきにくい |
| ステータス色の凡例がない | 色の意味が分からない |

### 4. 各シート固有の問題
| シート | 課題 |
|--------|------|
| Case_Master | ドリルダウンが右側に離れていて関係性が不明確 |
| Kanban_View | 列間のスペースが無駄、選択中のWBSが分かりにくい |
| Template/PRJ | ガントチャートの操作方法が不明 |

---

## 改善計画

### Phase 1: 即効性のある改善

#### 1.1 Homeシートの追加
**目的**: ダッシュボード＋ナビゲーションハブ

```
┌─────────────────────────────────────────────────────────────┐
│ 📊 Modern Excel PMS - ホーム                                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ 📈 サマリー                                                 │
│ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐           │
│ │ 案件数  │ │ 施策数  │ │ 完了率  │ │ 遅延    │           │
│ │   1     │ │   2     │ │  0%     │ │  0件    │           │
│ └─────────┘ └─────────┘ └─────────┘ └─────────┘           │
│                                                             │
│ 🔗 クイックアクセス                                         │
│ • Case_Master    - 案件一覧・ポートフォリオ管理            │
│ • Measure_Master - 施策一覧・WBSリンク                     │
│ • Kanban_View    - タスク状況ボード                        │
│ • Config         - 設定（担当者・祝日）                    │
│                                                             │
│ 📋 凡例                                                     │
│ 薄青セル = 入力可能  │  グレーセル = 自動計算              │
│                                                             │
│ ステータス: 🔵未着手  🟢進行中  🔴遅延  ⚪完了              │
└─────────────────────────────────────────────────────────────┘
```

**実装内容**:
- `home_sheet()` 関数を新規作成
- KPI サマリー（COUNTIF/SUMIF で集計）
- 各シートへのハイパーリンク
- 入力セル凡例とステータス色凡例

#### 1.2 入力セルの凡例追加
**目的**: どのセルが編集可能か一目で分かるように

**実装内容**:
- 全シートの行2に凡例を追加
- 「💡 薄青 = 入力可能 │ グレー = 自動計算」

#### 1.3 シート順の整理
**目的**: 作業フローに沿った論理的な順序

**変更前**:
```
Config → Template → PRJ_001 → PRJ_002 → Case_Master → Measure_Master → Kanban_View
```

**変更後**:
```
Home → Config → Case_Master → Measure_Master → Kanban_View → PRJ_001 → PRJ_002 → Template
```

**理由**:
1. Home: 最初に見るダッシュボード
2. Config: 初期設定（担当者・祝日）
3. Case_Master: 案件を登録
4. Measure_Master: 施策を登録・WBSリンク
5. Kanban_View: 日常的なタスク確認
6. PRJ_xxx: 詳細なWBS作業
7. Template: 通常は非表示（コピー元）

#### 1.4 進捗率を%入力に変更
**目的**: 直感的な入力体験

**変更前**:
- 入力: `0.5` → 表示: `50%`
- データバリデーション: 0〜1

**変更後**:
- 入力: `50` → 表示: `50%`
- データバリデーション: 0〜100
- 数式調整: `G5/100` で計算

---

### Phase 2: UX強化

#### 2.1 Case_Masterのレイアウト統合
**目的**: ドリルダウンを直下に配置し関係性を明確化

**変更前**:
```
┌──────────────────────┐  ┌──────────────────────┐
│ 案件一覧 (A-E列)     │  │ ドリルダウン (G-N列) │
└──────────────────────┘  └──────────────────────┘
```

**変更後**:
```
┌─────────────────────────────────────────────────┐
│ 📊 案件一覧                                     │
│ ┌─────────────────────────────────────────────┐ │
│ │ 案件ID │ 案件名 │ メモ │ 施策数 │ 平均進捗 │ │
│ └─────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────┤
│ 🔍 詳細表示: [案件ID選択 ▼]                     │
├─────────────────────────────────────────────────┤
│ 📋 選択した案件の施策一覧                       │
│ ┌─────────────────────────────────────────────┐ │
│ │ (FILTER でスピル表示)                       │ │
│ └─────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────┘
```

#### 2.2 Kanban_Viewの改善
**目的**: 列幅統一、選択中の施策情報を明示

**変更前**:
```
[B: To Do] [C: 空] [D: Doing] [E: 空] [F: Done] [G: 空] [H: 遅延]
```

**変更後**:
```
┌─────────────────────────────────────────────────────────────┐
│ 📋 カンバンボード                                           │
│ 選択中: PRJ_001 - 要件定義・設計フェーズ                    │
├──────────────┬──────────────┬──────────────┬───────────────┤
│ 📥 To Do (3) │ 🔄 Doing (2) │ ✅ Done (4)  │ ⚠️ 遅延 (0)   │
├──────────────┼──────────────┼──────────────┼───────────────┤
│ タスク1      │ タスク5      │ タスク9      │               │
│ 👤担当/📅期限│ 👤担当/📅期限│ 👤担当/📅期限│               │
├──────────────┼──────────────┼──────────────┼───────────────┤
│ タスク2      │ タスク6      │ タスク10     │               │
│ 👤担当/📅期限│ 👤担当/📅期限│ 👤担当/📅期限│               │
└──────────────┴──────────────┴──────────────┴───────────────┘
```

**実装内容**:
- 空白列（C, E, G）を削除
- 各列にタスク件数バッジを追加
- 選択中のWBS情報を行2-3に表示
- 列幅を均等化（25文字幅）

#### 2.3 パンくずナビゲーション
**目的**: 現在位置と親シートへの導線を明確化

**実装内容**:
- 各シートの行1にパンくずを追加
- 例: `Home > Case_Master > [現在のシート]`
- ハイパーリンクで遷移可能

---

## 実装スケジュール

### Phase 1（即効性のある改善）
| タスク | 対象ファイル | 優先度 |
|--------|-------------|--------|
| Homeシート追加 | build_workbook.py | 高 |
| 入力セル凡例追加 | build_workbook.py | 高 |
| シート順整理 | build_workbook.py | 高 |
| 進捗率%化 | build_workbook.py | 中 |

### Phase 2（UX強化）
| タスク | 対象ファイル | 優先度 |
|--------|-------------|--------|
| Case_Masterレイアウト | build_workbook.py | 中 |
| Kanban_View改善 | build_workbook.py | 中 |
| パンくずナビ | build_workbook.py | 低 |

---

## 技術的な変更点

### 新規関数
```python
def home_sheet(password_hash: str = "", m365_mode: bool = False) -> str:
    """Homeダッシュボードシートを生成する。"""
    # KPIサマリー、ナビゲーションリンク、凡例を含む
```

### 変更関数
```python
def build_workbook(...):
    # シート順序の変更
    sheet_names = ["Home", "Config", "Case_Master", "Measure_Master",
                   "Kanban_View"] + prj_sheets + ["Template"]

def template_cells(sample: bool = False):
    # 進捗率の入力範囲を 0-100 に変更
    # データバリデーションを更新

def case_master_sheet(...):
    # ドリルダウンを下部に配置

def kanban_sheet(...):
    # 列レイアウトを改善
    # 選択中WBS情報を追加
```

### 数式変更
```excel
# 進捗率計算（変更後）
全体進捗 = SUMPRODUCT(E5:E104, G5:G104/100) / SUM(E5:E104)

# ステータス判定（変更後）
=IFS(G5>=100, "完了", F5<TODAY(), "遅延", D5<=TODAY(), "進行中", TRUE, "未着手")
```

---

## 成功基準

| 指標 | 目標 |
|------|------|
| 初回利用時の迷い | Homeシートから5クリック以内で全機能にアクセス可能 |
| 入力エラー | 進捗率の入力ミスを50%削減 |
| ナビゲーション | シート間遷移が直感的に理解できる |
| 視覚的一貫性 | 全シートで統一されたスタイル |

---

## 備考

- 本計画はMicrosoft 365環境での利用を前提としています
- VBA版は廃止済み、FILTER/LET/SEQUENCEを活用
- SharePoint/Excel Onlineでの動作を保証

---

*作成日: 2024年12月14日*
*バージョン: 1.0*
